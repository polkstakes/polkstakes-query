# polkstakes-query
Subquery engine for polkstakes

## Generate schema types
```sh
npm run codegen
```

## Build the project
```sh
npm run build
```

## Start project
```sh
docker-compose pull && docker-compose up
```